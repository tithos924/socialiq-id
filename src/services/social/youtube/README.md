# youtube adapter

To be implemented in Phase 6 (Social Integrations), implementing the
`SocialProvider` interface from `../social-provider.interface.ts`.

Not yet implemented. Do not stub fake data here — leave unimplemented
until real OAuth credentials and API access are configured.
